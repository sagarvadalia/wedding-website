# Server Configuration
PORT=5001
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# MongoDB - Option 1: Individual credentials (recommended for production)
MONGO_USERNAME=
MONGO_PASSWORD=
MONGO_CLUSTER=

# MongoDB - Option 2: Full URI (for local development)
MONGODB_URI=mongodb://localhost:27017/wedding

# Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this
# Admin dashboard: token sent by client after password check (default: demo-token)
# ADMIN_TOKEN=demo-token

# RSVP deadline (optional). After this date RSVP is read-only. ISO date e.g. 2027-03-01
# RSVP_BY_DATE=

# Confirmation email (Resend). Set RESEND_API_KEY to enable.
# RESEND_API_KEY=
# EMAIL_FROM=Wedding <noreply@yourdomain.com>
# WEDDING_NAME=Sagar & Grace
# CONFIRMATION_EMAIL_ENABLED=true
